##############################
# Runtime dependencies
# (Pinned loosely by major version for stability; adjust if Render build shows conflicts.)
# If you prefer fully pinned versions, replace ~= with == after verifying locally.
##############################

# FastAPI core + Uvicorn server
fastapi~=0.115
uvicorn[standard]~=0.30

# Data validation (FastAPI will pull pydantic, but we declare explicitly for clarity)
pydantic~=2.9

# HTTP and async client libs (requests is used directly; httpx is a transitive dep for some SDKs but we pin it)
requests~=2.32
httpx~=0.27

# Date parsing for reviews
python-dateutil~=2.9

# Environment variable loader
python-dotenv~=1.0

# JSON performance (optional but FastAPI auto-detects orjson if present)
orjson~=3.10

# OpenAI & Google Gemini SDKs (Gemini optional; if unused you may remove)
openai~=1.0
google-generativeai~=0.7

# Supabase Python client (official package name 'supabase')
supabase~=2.0

# Scraping helpers (present or planned in scraper modules)
beautifulsoup4~=4.12
selectorlib~=0.16

##############################
# Testing / Dev dependencies
##############################
pytest~=8.3
pytest-asyncio~=0.23
pytest-cov~=5.0

##############################
# Notes:
# - Remove google-generativeai if you don't use Gemini to speed up cold starts.
# - fastapi[all] was replaced by explicit deps to keep image smaller; add 'python-multipart' if you later handle file uploads.
# - On Render, the start command should be: uvicorn main:app --host 0.0.0.0 --port $PORT
# - If any dependency resolution conflict appears, run: pip install --upgrade pip && pip install -r requirements.txt
# - For deterministic builds in production, freeze exact versions locally with: pip freeze > requirements.lock
